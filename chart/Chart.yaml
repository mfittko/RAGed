apiVersion: v2
name: rag-stack
description: Self-hosted RAG stack (API + Postgres + Ollama) with optional enrichment worker and MinIO blob storage
type: application
version: 1.0.0
appVersion: "1.0.0"
